<script src="https://cdn.tailwindcss.com"></script>

<div class="container mx-auto px-4 py-8">
    <h1 class="text-2xl font-bold mb-6">ユーザー一覧</h1>

    <div class="bg-white shadow rounded-lg overflow-hidden">
        <table class="min-w-full divide-y divide-gray-200">
            <thead class="bg-gray-50">
                <tr>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Email</th>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Actions</th>
                </tr>
            </thead>
            <tbody class="bg-white divide-y divide-gray-200">
                <% @es_tests.each do |es_test| %>
                <tr>
                    <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                        <%= es_test.email %>
                    </td>
                    <td class="px-6 py-4 whitespace-nowrap text-sm font-medium">
                        <%= link_to '詳細', show_event_sourcing_path(es_test), class: 'text-indigo-600 hover:text-indigo-900 mr-4' %>
                        <%= link_to 'パスワード変更', edit_event_sourcing_path(es_test), class: 'text-indigo-600 hover:text-indigo-900' %>
                    </td>
                </tr>
                <% end %>
            </tbody>
        </table>
    </div>
</div>