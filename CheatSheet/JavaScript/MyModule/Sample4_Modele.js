export class Class_1 {
  //コンストラクタで引数を受け取り
  //constructorは必須な関数（classが呼び出された時に最初に実行される関数）
  //a, bの部分は任意の引数
  //→constructorが最初に実行される関数なので、
  //引数で初期値を受け渡したりします。
  //また、引数の値を保持させるために、
  // class自身（this）に保存します。
  constructor(Hikisuu1) {
    this.Hikisuu1 = Hikisuu1;
  }
}
